spring.application.name=govportal
# Database
spring.datasource.url=${DATASOURCE_URL}
spring.datasource.username=${DATASOURCE_USERNAME}
spring.datasource.password=${DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

#Frontend/Cors Configuration
frontend.url=${FRONTEND_URL:http://localhost:3000}

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.format-sql=true

# JWT
jwt.secret=4cf7e4da6f1cc97a1fbafb744222be5a76ab3ca9388ff2fd9673a1f6cebfb86f
jwt.expiration=86400000

# File Upload
app.max-file-size=5242880 
app.allowed-file-types=image/jpeg,application/pdf

# Increase file size limits (adjust as needed)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.enabled=true

# Email Service
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=magartejas2001@gmail.com
spring.mail.password=fjpb qdjc rxfd cbcs
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


aws.s3.access.key.id=${AWS_S3_ACCESS_KEY_ID}
aws.s3.secret.access.key=${AWS_S3_SECRET_KEY}
aws.region=${AWS_REGION}
aws.s3.bucket-name=${AWS_S3_BUCKET_NAME}


#logging.level.root=ERROR
#logging.level.com.sunbeam=DEBUG

#logging.level.org.hibernate.SQL=DEBUG
#logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
